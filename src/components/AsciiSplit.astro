<div class="ascii-split" aria-hidden="true"></div>

<style>
  .ascii-split {
    --split-out: calc(-1 * var(--ascii-pad, var(--space-6)));
    height: 1px;
    margin: var(--space-6) var(--split-out);
    background: var(--color-line);
  }
</style>
